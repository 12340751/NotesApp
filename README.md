# NotesApp
Minimalistic offline desktop notes app built with Electron and JavaScript. Notes are stored as local Markdown files. Features live preview, tags, search, auto-save, light/dark mode, and a custom theme editor. Users can create and export themes as JSON files saved to the Desktop.
